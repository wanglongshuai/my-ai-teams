# 思琦 · 个人对话记忆

## 最近讨论过的话题

### P0 代码审查（正式审查完成）
对 P0 前端功能（@提及 / 消息引用 / Hover 工具栏）出具正式审查报告（2026-04-02）。

**审查结论：⚠️ 需要修改（1 个阻塞项 + 3 个建议）**

**阻塞项（必须修改）**
- `app.js:362`：`startReply` 内联 `onmousedown` 用单引号拼接参数，`escapeHtml` 不转义单引号，`.replace(/'/g, "\\'")` 在 HTML 属性中无效，含单引号内容会 break JS 语法，存在真实 XSS 向量
- 修复方案：改用 `data-*` 属性传参 + 事件委托，彻底绕开内联字符串拼接

**建议项（不阻塞合并）**
1. `app.js:393-398`：`marked.parse()` 未配置 sanitize，AI 消息当前风险可控，一旦用户可发 markdown 将成阻塞项
2. `app.js:354`：`@提及` 正则转义写法正确，但需测试 marked 是否会二次处理 span 内容
3. `app.js:361, 940-942`：`msgIdAttr` 为空时 `scrollToMsg` 会误匹配，建议空 id 时不渲染回复按钮或判空跳过

**肯定项**：`@提及` 正则转义规范、`showMentionDropdown` 无 XSS、`onmousedown` 细节处理正确、`reply_to ?? null` 兼容已做

已 @雨欣 要求修复阻塞项后再通知复审。

## 关于用户的了解
- 审查风格严谨，区分阻塞项与建议项，报告结构清晰
- 关注安全性（XSS、权限校验）和代码质量（内存泄漏）
- 与建平、浩然、雨欣协作紧密，习惯主动同步风险

## 待跟进的事项
- 等待雨欣修复 `startReply` 阻塞项后进行二次复审